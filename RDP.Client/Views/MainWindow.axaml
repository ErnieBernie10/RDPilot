<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:RDP.Client.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1440" d:DesignHeight="900"
        x:Class="RDP.Client.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/avalonia-logo.ico"
        Width="1440"
        Height="900"
        MinWidth="900"
        MinHeight="600"
        Title="RDP.Client">

    <Design.DataContext>
        <!-- This only sets the DataContext for the previewer in an IDE,
             to set the actual DataContext for runtime, set the DataContext property in code (look at App.axaml.cs) -->
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Spacing="5" Margin="5">
            <TextBox Text="{Binding Host}" Watermark="Host"/>
            <TextBox Text="{Binding Domain}" Watermark="Domain"/>
            <TextBox Text="{Binding Username}" Watermark="Username"/>
            <TextBox Text="{Binding Password}" Watermark="Password" PasswordChar="*"/>
            <TextBox Text="{Binding GatewayHost}" Watermark="Gateway host"/>
            <TextBox Text="{Binding GatewayDomain}" Watermark="Gateway domain"/>
            <TextBox Text="{Binding GatewayUsername}" Watermark="Gateway Username"/>
            <TextBox Text="{Binding GatewayPassword}" Watermark="Gateway Password" PasswordChar="*"/>
            <Button Content="Connect" Command="{Binding ConnectCommand}"/>
            <Button Content="Disconnect" Command="{Binding DisconnectCommand}"/>
        </StackPanel>
        <ScrollViewer Name="RdpScrollViewer"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      SizeChanged="OnRdpViewportSizeChanged">
            <Image Source="{Binding Screen}" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top"
                   PointerMoved="OnPointerMoved"
                   PointerPressed="OnPointerPressed"
                   PointerReleased="OnPointerReleased"
                   PointerWheelChanged="OnPointerWheelChanged"
                   Focusable="True"
                   Name="RdpImage"/>
        </ScrollViewer>
    </DockPanel>
</Window>
